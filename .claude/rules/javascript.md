## JavaScript spec

Target **ES2024+** (latest stable) in main, renderer and shared. Use modern syntax: `structuredClone`, `Promise.withResolvers`, `Array.toSorted/toReversed/findLast`, `Object.groupBy`, logical assignment operators, `at()`, etc.
